# Environment Variables Template
# Copy to .env and fill in your actual values

# Development environment
NODE_ENV=development

# ===========================================
# Authentication (AWS Cognito + NextAuth.js)
# ===========================================

# AWS Cognito Configuration
# Get these from your AWS Cognito User Pool settings
COGNITO_CLIENT_ID=your_cognito_app_client_id
COGNITO_CLIENT_SECRET=your_cognito_app_client_secret
# Format: https://cognito-idp.{region}.amazonaws.com/{userPoolId}
COGNITO_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_XXXXXXXXX

# NextAuth.js Configuration
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here
# The base URL of your application (no trailing slash)
NEXTAUTH_URL=http://localhost:3000

# Editor password for simple auth fallback (when Cognito is not configured)
EDITOR_PASSWORD=your_editor_password_here

# ===========================================
# AWS Services
# ===========================================

# AWS Region (defaults to us-east-1)
AWS_REGION=us-east-1

# DynamoDB table name (defaults to jhr-content)
DYNAMODB_TABLE_NAME=jhr-website-content

# S3 bucket for media assets (defaults to jhr-photography-assets)
S3_BUCKET_NAME=jhr-website-images

# CloudFront CDN domain for serving S3 assets
CLOUDFRONT_DOMAIN=your_cloudfront_domain.cloudfront.net

# ===========================================
# Blog System
# ===========================================

# API key for blog upload endpoint (used by n8n automation)
# Generate with: openssl rand -hex 32
BLOG_API_KEY=your_blog_api_key_here

# OpenAI API key for AI-powered SEO/GEO metadata generation on blog posts
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here